allOf:
  - $ref: '#/definitions/notify.BaseEvent'
  - type: object
    required: [Event, EventKey]
    properties:
      Event:
        type: string
        description: 事件类型
        enum: [CLICK]
      EventKey:
        type: string
        description: 事件KEY值，与自定义菜单接口中KEY值对应
