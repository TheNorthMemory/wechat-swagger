allOf:
  - $ref: '#/definitions/notify.BaseEvent'
  - type: object
    required: [Event, PageId, OrderId]
    properties:
      Event:
        type: string
        description: 事件类型
        enum: [giftcard_pay_done]
      PageId:
        type: string
        description: 货架的id
      OrderId:
        type: string
        description: 订单号
