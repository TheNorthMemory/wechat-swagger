allOf:
  - $ref: '#/definitions/notify.BaseEvent'
  - type: object
    required: [Event, Latitude, Longitude, Precision]
    properties:
      Event:
        type: string
        description: 事件类型
        enum: [LOCATION]
      Latitude:
        type: number
        format: float
        description: 地理位置纬度
      Longitude:
        type: number
        format: float
        description: 地理位置经度
      Precision:
        type: number
        format: float
        description: 地理位置精度
