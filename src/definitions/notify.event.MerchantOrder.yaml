allOf:
  - $ref: '#/definitions/notify.BaseEvent'
  - type: object
    required: [Event, OrderId, OrderStatus, ProductId, SkuInfo]
    properties:
      Event:
        type: string
        description: 事件类型
        enum: [merchant_order]
      OrderId:
        type: string
      OrderStatus:
        type: integer
        format: int32
      ProductId:
        type: string
      SkuInfo:
        type: string
