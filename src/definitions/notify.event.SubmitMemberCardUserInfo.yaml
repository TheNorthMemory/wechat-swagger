allOf:
  - $ref: '#/definitions/notify.BaseEvent'
  - type: object
    required: [Event, CardId, Detail]
    properties:
      Event:
        type: string
        description: 事件类型
        enum: [submit_membercard_user_info]
      CardId:
        type: string
        description: 卡券ID。
      UserCardCode:
        type: string
        description: code序列号。
