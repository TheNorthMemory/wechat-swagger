allOf:
  - $ref: '#/definitions/notify.BaseMessage'
  - type: object
    required: [MsgType, Content]
    properties:
      MsgType:
        type: string
        enum: [text]
      Content:
        type: string
        description: 文本消息内容
