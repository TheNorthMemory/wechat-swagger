allOf:
  - $ref: '#/definitions/notify.Base'
  - type: object
    required: [MsgType]
    properties:
      MsgType:
        type: string
        enum: [transfer_customer_service]
      TransInfo:
        type: object
        properties:
          KfAccount:
            type: string
            description: 指定会话接入的客服账号
