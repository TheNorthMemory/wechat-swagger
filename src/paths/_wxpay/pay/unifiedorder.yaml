post:
  tags:
    - 支付∙JSAPI支付
  summary: 统一下单
  description: '除付款码支付场景以外，商户系统先调用该接口在微信支付服务后台生成预支付交易单，返回正确的预支付交易会话标识后再按Native、JSAPI、APP等不同场景生成交易串调起支付。[官方文档](https://pay.weixin.qq.com/wiki/doc/api/jsapi.php?chapter=9_1)'
  consumes:
    - application/xml
  produces:
    - application/xml
  parameters:
    - name: buffer
      in: body
      required: true
      schema:
        $ref: 'wxpay.yaml#/unifiedorder.request'
  responses:
    '200':
      description: ''
      schema:
        $ref: 'wxpay.yaml#/unifiedorder.response'
