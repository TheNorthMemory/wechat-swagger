get:
  summary: 检验授权凭证（access_token）是否有效
  operationId: sns/auth
  tags:
    - 网页授权
  security:
    - accessToken: []
  parameters:
    - in: query
      name: openid
      type: string
      description: 用户的唯一标识
      required: true
  responses:
    '200':
      description: ''
